import type { ProviderConfig } from "../types";

export const qoderConfig: ProviderConfig = {
  transport: "qoder",
  baseUrl: "https://api3.qoder.sh",
  authType: "apikey",
  authHeader: "Authorization",
};
