import type { ProviderConfig } from "../types";

export const mimoConfig: ProviderConfig = {
  transport: "mimo",
  baseUrl: "https://api.xiaomimimo.com/v1",
  authType: "apikey",
  authHeader: "Authorization",
};
